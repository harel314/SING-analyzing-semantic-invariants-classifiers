# Notebooks (planned)

Planned notebooks:
- `01_single_image_quickstart.ipynb`
  - installs requirements and package
  - loads one image
  - runs `resnet` + `dinovit1` end-to-end with existing SING APIs
  - computes and prints IS, AS(classes vector), AS(attributes vector)
  - plots top-|AS| bars and IS comparison
- `02_seed_sweep_visualization.ipynb`

They should call package APIs from `sing`, not duplicate core logic.
