# Dataset Note (v1)

v1 does not require a dataset for core functionality.

- Input is a single user image.
- Model and translator assets are external.
- Optional dictionary embedding files are shipped in `misc/` as placeholders.
